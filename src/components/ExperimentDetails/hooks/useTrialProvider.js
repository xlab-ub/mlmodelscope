import {useState} from "react";

export default function trialProvider(experiment) {
  const [inputs, setInputs] = useState([]);
  const [trials, setTrials] = useState([]);
  const [currentInput, setCurrentInput] = useState(0);


}
